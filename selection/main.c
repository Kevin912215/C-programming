#include <stdio.h>
#include <stdlib.h>

int main()
{
    int w;
    printf("Please enter a number:");
    scanf("%d",&w);
    if (w < 10)
       {
        printf("small");
        printf("small");
       }
    else
        printf("big");
    return 0;
}
